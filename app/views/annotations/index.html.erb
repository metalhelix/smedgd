<% if !params[:search] %>
<% else %>
  <%= link_to 'restart', features_path, :class => 'btn' %>
  <br/><br/>
  <% if @annotations.size > 0 %>
    <%= render :partial => 'annotations/go_list', :locals => {:annotations => @annotations} %>
  <% else %>
  <% end %>
<% end %>


<br />
<%= link_to 'restart', features_path, :class => 'btn' %>

